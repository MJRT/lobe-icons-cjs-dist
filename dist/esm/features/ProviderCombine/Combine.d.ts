import { ReactNode } from 'react';
import { DivProps } from '../providerConfig';
declare const Combine: import("react").NamedExoticComponent<DivProps & {
    left: ReactNode;
    right: ReactNode;
    size: number;
}>;
export default Combine;
